
<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <% unless @task.new_record? %>
    <%= f.label :completed %>
    <%= f.check_box :completed, {}, 'Yes', 'No' %>
  <% end %>
  <%# REMEMBER YOU CAN USE ANY RUBY CODE - INCLUDING IF/UNLESS STATEMENTS ^^ %>
  <%= f.submit %>
<% end %>

<%= link_to 'Return to task list', tasks_path  %>
